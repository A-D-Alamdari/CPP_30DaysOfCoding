# include <iostream>
# include <cmath>

using namespace std;

int main() {

    double x = 3.0;
    double y = 4.0;
    double z;

    // z = max(x, y);
    // z = min(x, y);

    // z = pow(x, y);

    // z = sqrt(y);

    // z = round (4 /3);

    // z = floor(3 / 4);

    z = ceil(y / x);

    cout << z;

    return 0;
}